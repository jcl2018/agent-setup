interface:
  display_name: "Lv0 Instruction Core"
  short_description: "Route work through the shared stack"
  default_prompt: "Use $lv0-instruction-core to choose the right shared stack, route context through .ai_shared, keep wrapper-specific behavior thin, and summarize the validation path."

policy:
  allow_implicit_invocation: true
