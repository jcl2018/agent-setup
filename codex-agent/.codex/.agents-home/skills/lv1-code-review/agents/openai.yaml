interface:
  display_name: "Lv1 Code Review"
  short_description: "Review changes for bugs, regressions, and test gaps"
  default_prompt: "Use $lv1-code-review to inspect the diff or file set, surface the highest-risk findings first, call out missing validation, and summarize residual risk."

policy:
  allow_implicit_invocation: true
