interface:
  display_name: "Lv0 Skill Onboarding"
  short_description: "Author or extend shared skills and agents safely"
  default_prompt: "Use $lv0-skill-onboarding to choose the right layer, update canonical shared skill definitions in .ai_shared when needed, regenerate the tool wrappers, and summarize the validation."

policy:
  allow_implicit_invocation: true
