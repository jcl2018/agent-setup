# Progress Tracker

## Purpose
Capture completed or meaningful in-progress work so future sessions can resume without rebuilding context from scratch.

## Update Rules
- Add the newest entry at the top of `## Entries`.
- Record the goal, the main changes or findings, validation, and any useful resume cues.
- Keep unfinished work out of this file; move it into `future-plan.md`.
- Keep one shared tracker in `.ai_shared/knowledge/` for this repo instead of mirroring the same note across multiple tool-root knowledge folders.

## Entries

### 2026-03-12
- Goal: force a fresh weekly-style `lv0-home-auditor` run for this repo after the earlier drift remediation, as if no current-week audit entry existed yet.
- Changed: re-ran the global home-scope checks and this repo's repo-specific mirror checks; confirmed the live home roots, required local tools, managed root files, shared layer, Codex wrappers, Codex home-library wrappers, and Claude wrappers all align with the repo-managed surfaces; confirmed `lv1-figma-implement-design` is now registered in the catalog and advertised in the entry docs; and confirmed the shared local-repo audit baseline now treats this repo's documented validation commands as sufficient, leaving only one nice-to-have note about the lack of a `docs/` or `examples/` folder.
- Validation: verified `/Users/chjiang/.ai_shared`, `/Users/chjiang/.codex`, `/Users/chjiang/.claude`, and `/Users/chjiang/AGENTS.md` exist; verified `codex`, `claude`, `pwsh`, and `git` are runnable; ran `pwsh -NoLogo -NoProfile -File ./.ai_shared/skills/lv0-home-auditor/scripts/audit-repo.ps1 -RepoPath .`; ran `diff -rq` across `.ai_shared`, `.codex/skills`, `.codex/.agents-home`, and `.claude` with the managed-mirror exclusions and got no output; and verified `lv1-figma-implement-design` exists in `catalog.json`.
- Resume cues: if someone enters this repo later in the same week and asks whether the home config is okay or whether the repo satisfies its local rules, the current answer is yes for the managed surfaces; the only remaining repo-audit note is the optional `docs/` or `examples/` folder.

### 2026-03-12
- Goal: remediate the live-home mirror drift and the repo-local discovery drift that the explicit `lv0-home-auditor` pass found earlier on March 12, 2026.
- Changed: synced the canonical shared `lv0-home-auditor` and `lv0-skill-onboarding` source directories into `/Users/chjiang/.ai_shared/skills/`; removed the stale live-home `lv1-github-repo-readiness/scripts/audit-repo.ps1` file from the shared layer plus the Codex, Codex home-library, and Claude wrapper script folders; registered `lv1-figma-implement-design` in `catalog.json`, `AGENTS.md`, `.codex/AGENTS.md`, `.claude/CLAUDE.md`, and `agent-stack.md` so the active on-disk skill is now discoverable again; updated the shared local-repo checklist and audit helper so a documented validation-command path counts for repos like this mirror; refreshed the local `lv0-home-auditor` wrapper asset directories from the canonical shared skill; and synced the touched managed files into `/Users/chjiang` with shell `rsync`.
- Validation: ran `python3 -m json.tool .ai_shared/skills/catalog.json`; ran `pwsh -NoLogo -NoProfile -File ./.ai_shared/skills/lv0-home-auditor/scripts/audit-repo.ps1 -RepoPath .`; reran `diff -rq` across `.ai_shared`, `.codex/skills`, `.codex/.agents-home`, and `.claude` with the managed-mirror exclusions and got no remaining drift; verified `lv1-figma-implement-design` now appears in the repo and live-home copies of `AGENTS.md`, `.codex/AGENTS.md`, `.claude/CLAUDE.md`, and `catalog.json`; and confirmed the stale live-home `audit-repo.ps1` files are gone from all four GitHub-readiness script directories.
- Resume cues: the repo and `/Users/chjiang` now converge on the managed surfaces again, and the repo-audit baseline now recognizes this mirror's documented validation commands; the remaining practical gap is still the macOS PowerShell sync issue, since `rsync` was used again to guarantee the live-home repair.

### 2026-03-12
- Goal: run an explicit `lv0-home-auditor` pass for both the global home scope and this repo's repo-specific mirror rules.
- Changed: the current-week global and repo-local audit gates were already satisfied, but the audit was re-run on direct request; confirmed the live home folders and required local tools are accessible; confirmed the managed root and wrapper entry files still match between the repo and `/Users/chjiang`; found repo-specific mirror drift because the live home still has older copies of `.ai_shared/skills/lv0-home-auditor/shared.md` and `.ai_shared/skills/lv0-skill-onboarding/shared.md`, and because the deleted `audit-repo.ps1` file still exists under the live-home `lv1-github-repo-readiness` script folders; the shared local-repo audit baseline found no blockers but did flag the lack of an obvious automated test path plus the absence of a `docs/` or `examples/` folder; reconfirmed internal discovery drift because `lv1-figma-implement-design` exists in the shared and wrapper skill directories but is omitted from `catalog.json`, `AGENTS.md`, `.codex/AGENTS.md`, and `.claude/CLAUDE.md`; and noted `/Users/chjiang/.codex/vendor_imports/skills` remains an unmanaged cleanup candidate outside the mirror contract.
- Validation: read the `lv0-home-auditor` skill, home-audit workflow, checklist, repo-local audit rules, and both progress trackers; ran `pwsh -NoLogo -NoProfile -File ./.ai_shared/skills/lv0-home-auditor/scripts/audit-repo.ps1 -RepoPath .`; verified `/Users/chjiang/.ai_shared`, `/Users/chjiang/.codex`, `/Users/chjiang/.claude`, and `/Users/chjiang/AGENTS.md` exist; verified `codex`, `claude`, `pwsh`, and `git` are runnable; compared `AGENTS.md`, `.codex/AGENTS.md`, `.codex/config.toml`, `.claude/CLAUDE.md`, `.claude/settings.json`, and `.ai_shared/skills/catalog.json` between the repo and `/Users/chjiang`; ran `diff -rq` across `.ai_shared`, `.codex/skills`, `.codex/.agents-home`, and `.claude` with the managed-mirror exclusions; compared the repo and live-home copies of `lv0-home-auditor/shared.md` and `lv0-skill-onboarding/shared.md`; and verified that `lv1-figma-implement-design` is present on disk but omitted from the catalog and entry docs.
- Resume cues: sync the canonical shared-skill source files into `/Users/chjiang/.ai_shared/skills/`, remove the stale live-home `audit-repo.ps1` files from the GitHub-readiness wrapper stacks, decide whether `lv1-figma-implement-design` is active and either register or retire it consistently, and document at least one automated test or explicitly state that this repo's validation is manual.

### 2026-03-12
- Goal: clarify the big-picture layer contract so `lv0` means shared logistics/standards/audit/automation work, `lv1` means reusable shared workflows, and repo-specific behavior uses repo-prefixed `lv2-<repo>-*` wrappers.
- Changed: updated the canonical `lv0-home-auditor` and `lv0-skill-onboarding` skill definitions so they explicitly route layer decisions through that split; refreshed `agent-stack.md`, `agent-authoring.md`, and `naming-conventions.md` to document the same contract; updated the root Codex and Claude entry docs plus the shared skill catalog descriptions so the layer meanings are visible from both the stack docs and the user-facing wrappers; regenerated the affected Codex and Claude wrappers for `lv0-home-auditor` and `lv0-skill-onboarding`; restored the `lv0-home-auditor` wrapper asset directories after the PowerShell generator skipped them on this machine; and synced the touched managed files into `/Users/chjiang` with the shell mirror fallback.
- Validation: ran `python3 -m json.tool .ai_shared/skills/catalog.json`; read back the updated shared skill docs plus `agent-stack.md`, `agent-authoring.md`, `naming-conventions.md`, and `AGENTS.md`; ran `pwsh -NoLogo -NoProfile -File ./scripts/sync-shared-skills.ps1 -Name lv0-home-auditor,lv0-skill-onboarding -RepoRoot .`; restored the missing `lv0-home-auditor` wrapper `references/` and `scripts/` directories with shell `rsync`; synced the touched managed files into `/Users/chjiang` with shell `rsync`; and verified repo-versus-home equality for the touched entry docs, shared knowledge files, and generated skill wrappers.
- Resume cues: use `lv0` only for operational/logistics layers, keep reusable task flows in `lv1`, and name repo-specific wrappers `lv2-<repo>-*` so their scope stays obvious during discovery and maintenance.

### 2026-03-12
- Goal: move the generic repo-audit logic out of `lv1-github-repo-readiness` and into `lv0-home-auditor`, since that audit baseline is local-repo logic rather than GitHub-specific behavior.
- Changed: updated the canonical shared `lv0-home-auditor` skill, workflow, and checklist so repo-scoped weekly audits now run a shared local-repo audit baseline in addition to the home-alignment checks; added `lv0-home-auditor/references/local-repo-checklist.md` plus `lv0-home-auditor/scripts/audit-repo.ps1` for generic repo-hygiene checks such as tracked-artifact risk, README/test presence, large files, and secret-like literals; trimmed `lv1-github-repo-readiness` so it now builds on the latest `lv0-home-auditor` findings and focuses on GitHub-facing collaboration/publication checks; replaced `lv1-github-repo-readiness/scripts/audit-repo.ps1` with `scripts/audit-github-readiness.ps1`; refreshed the shared catalog, stack/authoring docs, root entry docs, and generated Codex/Claude wrappers; restored the wrapper asset directories with shell `rsync` after the PowerShell generator dropped them on this machine; and synced the updated managed repo surfaces into `/Users/chjiang` with the shell mirror fallback.
- Validation: ran `python3 -m json.tool .ai_shared/skills/catalog.json`; ran `pwsh -NoLogo -NoProfile -File ./.ai_shared/skills/lv0-home-auditor/scripts/audit-repo.ps1 -RepoPath .`; ran `pwsh -NoLogo -NoProfile -File ./.ai_shared/skills/lv1-github-repo-readiness/scripts/audit-github-readiness.ps1 -RepoPath . -Visibility either`; ran `pwsh -NoLogo -NoProfile -File ./scripts/sync-shared-skills.ps1 -Name lv0-home-auditor,lv1-github-repo-readiness -RepoRoot .`; restored the generated wrapper asset directories with `rsync`; scanned the repo for the new `audit-github-readiness.ps1`, `local-repo-checklist.md`, and `lv0-home-auditor` findings references; and verified repo-versus-home equality for the touched shared files plus the generated Codex and Claude wrapper files and assets.
- Resume cues: `lv0-home-auditor` now owns the shared local-repo audit baseline, while `lv1-github-repo-readiness` starts from those findings and stays focused on GitHub/publication-specific checks; the remaining tooling gap is still the macOS PowerShell path issue, which causes `sync-shared-skills.ps1` and the mirror helpers to drop or skip asset directories unless they are repaired with shell `rsync`.

### 2026-03-12
- Goal: extend `lv0-home-auditor` so it runs both the global weekly home audit and a repo-specific weekly audit, and teach this mirror repo to treat any managed repo-to-home discrepancy as audit drift.
- Changed: updated the canonical shared `lv0-home-auditor` and `lv0-instruction-core` definitions so the weekly gate now checks both `~/.ai_shared/knowledge/progress-tracker.md` and the current repo's `.ai_shared/knowledge/progress-tracker.md`; refreshed the shared home-audit workflow, checklist, catalog entry, stack docs, authoring guidance, naming guidance, and the root Codex and Claude entry docs so repo-specific audit rules and per-repo weekly audits are part of the default routing; added `.ai_shared/knowledge/home-audit-rules.md` for this repo so weekly repo audits compare the managed mirror surfaces against the live home paths and record any discrepancy; regenerated the Codex and Claude wrappers for `lv0-home-auditor` and `lv0-instruction-core`; restored the `lv1-github-repo-readiness` wrapper asset files after the full PowerShell regeneration path dropped them on this machine; and synced the managed repo surfaces into `/Users/chjiang` with a shell `rsync` fallback because the PowerShell mirror helpers did not update the live home root correctly on macOS.
- Validation: read `~/.codex/skills/lv0-skill-onboarding/SKILL.md`, `~/.ai_shared/knowledge/agent-authoring.md`, `~/.ai_shared/knowledge/agent-stack.md`, the shared audit skill definitions, `workflow-home-audit.md`, `home-audit-checklist.md`, and `scripts/home-mirror.ps1`; ran `python3 -m json.tool .ai_shared/skills/catalog.json`; ran `pwsh -NoLogo -NoProfile -File ./scripts/sync-shared-skills.ps1 -Name lv0-instruction-core,lv0-home-auditor -RepoRoot .`; confirmed the regenerated wrapper text in `.codex/skills/` and `.claude/skills/`; observed that `pwsh` could not resolve `./.ai_shared/skills` reliably on this machine and that `scripts/home-mirror.ps1` failed before copying the shared layer; restored the missing `lv1-github-repo-readiness` wrapper assets with `rsync`; synced `.ai_shared`, `.codex`, `.claude`, and `AGENTS.md` into `/Users/chjiang` with shell `rsync` while preserving the home-level continuity-file exclusions; and verified repo-versus-home equality for the touched entry docs, wrapper files, workflow, checklist, and new audit-rules note.
- Resume cues: the weekly audit stack now has two gates, with the repo-local gate recorded in `.ai_shared/knowledge/progress-tracker.md`; repo-specific audit rules now live in `.ai_shared/workflows/workflow-home-audit.md`, `.ai_shared/checklists/home-audit-checklist.md`, and optional `.ai_shared/knowledge/home-audit-rules.md`; and the remaining practical gap is the macOS PowerShell sync bug, which still needs investigation before the repo can rely on `scripts/sync-to-home.ps1` again on this machine.

### 2026-03-12
- Goal: audit whether the live home-root installation under `/Users/chjiang` is still consistent with this repo mirror.
- Changed: compared the managed shared layer, Codex and Claude wrapper surfaces, and root entry docs against the live home paths. Confirmed the repo and home copies of `AGENTS.md`, `.codex/AGENTS.md`, `.codex/config.toml`, `.claude/CLAUDE.md`, `.claude/settings.json`, and `.ai_shared/skills/catalog.json` still match, but found live-home drift in `.ai_shared/knowledge/agent-authoring.md`, `architecture.md`, `repo-map.md`, and `test-commands.md`; found the live home is missing the repo's `lv1-figma-implement-design` skill directories across `.ai_shared`, `.codex`, `.codex/.agents-home`, and `.claude`; and found a recreated unmanaged runtime checkout under `~/.codex/vendor_imports/skills`. Also reconfirmed a repo-local discovery mismatch: the repo still contains `lv1-figma-implement-design` files even though the catalog and entry docs do not mention the skill.
- Validation: read the home-audit skill, workflow, checklist, and `scripts/home-mirror.ps1`; verified the existence of `~/.ai_shared`, `~/.codex`, `~/.claude`, and `~/AGENTS.md`; ran `diff -u` on the managed root and wrapper entry files; ran `diff -rq` against `.ai_shared`, `.codex/skills`, `.codex/.agents-home`, and `.claude` with the sync exclusions; scanned repo and home paths for `lv1-figma-implement-design`; and listed `~/.codex/vendor_imports`.
- Resume cues: if the live home should match the repo again, sync the refreshed knowledge docs into `~/.ai_shared/knowledge`, decide whether `lv1-figma-implement-design` should exist or be retired across both the repo and home, and then review whether `~/.codex/vendor_imports/skills` should remain as runtime cache or be cleaned.

### 2026-03-11
- Goal: move the weekly `lv0-home-auditor` gate from repo-local continuity into the live home shared layer so it runs globally across repos once per calendar week.
- Changed: updated the canonical shared routing docs, `lv0-home-auditor`, `lv0-instruction-core`, the shared home-audit workflow and checklist, and the top-level Codex and Claude entry docs so the weekly audit gate now checks `~/.ai_shared/knowledge/progress-tracker.md` instead of the current repo's `.ai_shared/knowledge/progress-tracker.md`; clarified that repo-local progress trackers remain for repo continuity while the home-level `progress-tracker.md` and `future-plan.md` are reserved for global home-audit continuity; regenerated the Codex and Claude wrappers and synced the live home copies; and seeded `C:\Users\chang\.ai_shared\knowledge\progress-tracker.md` with a current-week `lv0-home-auditor` entry so this week's audit state is already global.
- Validation: read the existing shared routing docs plus `scripts/home-mirror.ps1`; updated the shared sources; ran `powershell -ExecutionPolicy Bypass -Command "& { & '.\scripts\sync-shared-skills.ps1' -Name 'lv0-instruction-core','lv0-home-auditor' -RepoRoot 'E:\projects\agent-setup' }"`; ran `powershell -ExecutionPolicy Bypass -Command "& { & '.\scripts\home-mirror.ps1' -Direction 'to-home' -Tool 'codex','claude' -RepoRoot 'E:\projects\agent-setup' -HomeRoot 'C:\Users\chang' }"` after regeneration completed; scanned the repo for stale repo-local weekly-audit references; confirmed the live Codex and Claude `AGENTS` and `SKILL.md` files now reference `~/.ai_shared/knowledge/progress-tracker.md`; confirmed repo-versus-live text equality for the updated Codex and Claude entry docs plus `lv0-home-auditor` and `lv0-instruction-core`; and verified the live home tracker contains the new current-week audit entry.
- Resume cues: the next repo you open in a new calendar week should use the home-level weekly gate rather than this backup repo's tracker; repo-local `.ai_shared/knowledge/progress-tracker.md` files still exist for repo continuity, but they no longer control whether `lv0-home-auditor` runs.

### 2026-03-11
- Goal: run an explicit audit of the new `lv0-run-transparency` behavior and verify the repo and live-home Codex and Claude surfaces are aligned.
- Changed: performed a findings-first audit of the shared routing docs, the new `lv0-run-transparency` skill, the generated Codex and Claude wrappers, and the live home copies under `C:\Users\chang\`; verified the weekly audit state, the review workflow baseline, the home-audit checklist, the on-disk references to `lv0-run-transparency`, and raw-text equality between the repo and live-home copies of the key entry docs and skill files. No code changes were required beyond recording the audit result.
- Validation: read `.ai_shared/knowledge/progress-tracker.md`, `C:\Users\chang\.ai_shared\workflows\workflow-code-review.md`, `C:\Users\chang\.ai_shared\checklists\home-audit-checklist.md`, `.ai_shared\knowledge\agent-stack.md`, `scripts\sync-to-home.ps1`, and `scripts\home-mirror.ps1`; scanned `AGENTS.md`, `.ai_shared`, `.codex`, and `.claude` for `lv0-run-transparency`; confirmed repo-versus-live text equality for `AGENTS.md`, `.codex\AGENTS.md`, `.claude\CLAUDE.md`, `.codex\skills\lv0-run-transparency\SKILL.md`, `.claude\skills\lv0-run-transparency\SKILL.md`, `.codex\skills\lv0-instruction-core\SKILL.md`, and `.claude\skills\lv0-instruction-core\SKILL.md`; and confirmed the shared home roots remain readable.
- Resume cues: the explicit audit found the new transparency layer correctly documented and synced for both Codex and Claude; the remaining practical caveat is that this is still instruction-driven behavior, so future runs depend on the agent following the stack rather than a hard-coded executable hook.

### 2026-03-11
- Goal: add a shared start-of-run transparency layer so each new run explains the selected skills, the repo-local and home-level shared context sources, and why that stack fits the task.
- Changed: added the canonical shared `lv0-run-transparency` skill under `.ai_shared/skills/` and registered it in `.ai_shared/skills/catalog.json`; updated the shared routing and stack docs so `lv0-run-transparency` is part of the default run-start stack through `lv0-instruction-core`; refreshed `agent-stack.md`, `agent-authoring.md`, `naming-conventions.md`, `AGENTS.md`, `.codex/AGENTS.md`, and `.claude/CLAUDE.md` so the new behavior and skill are documented; regenerated the Codex and Claude wrappers, including the Codex `.agents-home` mirror; and synced the updated shared layer plus both live home tool roots under `C:\Users\chang\`.
- Validation: `powershell -ExecutionPolicy Bypass -Command "& { & '.\scripts\sync-shared-skills.ps1' -Name 'lv0-instruction-core','lv0-run-transparency' -RepoRoot 'E:\projects\agent-setup' }"`; `powershell -ExecutionPolicy Bypass -Command "& { & '.\scripts\sync-to-home.ps1' -Tool 'codex','claude' -RepoRoot 'E:\projects\agent-setup' -HomeRoot 'C:\Users\chang' -WhatIf }"`; stale-reference and presence scans for `lv0-run-transparency` across the repo; `powershell -ExecutionPolicy Bypass -Command "& { & '.\scripts\home-mirror.ps1' -Direction 'to-home' -Tool 'codex','claude' -RepoRoot 'E:\projects\agent-setup' -HomeRoot 'C:\Users\chang' }"`; line checks confirming the new skill and start-of-run wording in the repo and live-home Codex and Claude skill files plus entry docs; and raw-text comparisons confirming the repo and live-home copies now match for the new skill, `lv0-instruction-core`, and the Codex and Claude entry docs.
- Resume cues: new runs should now start by naming the selected skills, the repo-local `.ai_shared/` sources consulted, the home-level `~/.ai_shared/` defaults consulted, and why that stack fits; when syncing live home roots, prefer the explicit `home-mirror.ps1` array form for `-Tool 'codex','claude'` because it reliably updates both targets, including newly added skills.

### 2026-03-11
- Goal: make `lv0-code-polisher` part of every code-editing stack by default and confirm the change landed for both Codex and Claude.
- Changed: updated the canonical shared routing, stack map, feature and defect skill definitions, and shared feature/defect workflows so any task that changes code now layers in `lv0-code-polisher`; refreshed the repo entry docs in `AGENTS.md`, `.codex/AGENTS.md`, and `.claude/CLAUDE.md` to state the new rule explicitly; regenerated the Codex and Claude skill wrappers, including the Codex `.agents-home` mirror; and synced the shared layer plus both live home tool roots under `C:\Users\chang\`.
- Validation: `powershell -ExecutionPolicy Bypass -File .\scripts\sync-shared-skills.ps1 -Name lv0-instruction-core,lv1-feature-dev,lv1-defect-fix`; `powershell -ExecutionPolicy Bypass -Command "& { & '.\scripts\home-mirror.ps1' -Direction 'to-home' -Tool 'codex','claude' -RepoRoot 'E:\projects\agent-setup' -HomeRoot 'C:\Users\chang' }"`; line checks confirming the new `lv0-code-polisher` wording in the repo and live-home Codex and Claude entry docs plus the regenerated `lv0-instruction-core`, `lv1-feature-dev`, and `lv1-defect-fix` skill files; and raw-text comparisons confirming the repo and live-home copies now match for those Codex and Claude files.
- Resume cues: code-editing tasks should now pick up `lv0-code-polisher` automatically through the shared entry layer and the feature/defect task docs; if another stack rule changes later, use the explicit PowerShell array form for `-Tool 'codex','claude'` when syncing to the live home roots so both tool targets update in one pass.

### 2026-03-10
- Goal: retire the managed `.github` wrapper surface so the shared skill stack only targets Codex and Claude.
- Changed: updated the canonical shared skill definitions, authoring docs, audit workflow, repo docs, and sync scripts so the active managed tool set is now Codex plus Claude; regenerated the Codex and Claude skill wrappers from `.ai_shared/skills/`; and removed the repo's legacy `.github` wrapper files so stale third-tool references are not regenerated.
- Validation: `powershell -ExecutionPolicy Bypass -File .\scripts\sync-shared-skills.ps1`, `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1 -WhatIf`, and a stale-reference scan for `copilot|copliot` across the repo excluding `.git` and the historical `progress-tracker.md` entries.
- Resume cues: the repo now behaves as a two-tool setup; the remaining optional cleanup is to remove any old live-home `.github` wrapper files manually if you want the home folder to match the repo exactly, because the sync scripts do not delete unmanaged content.

### 2026-03-10
- Goal: fix the remaining `sync-shared-skills.ps1` regeneration failure that blocked reliable canonical wrapper generation.
- Changed: updated `scripts/sync-shared-skills.ps1` so `Resolve-RequestedNames` now normalizes comma-separated input, de-duplicates names, and returns a real object array instead of wrapping a generic list object with `@($resolved)`, which had been causing the `Argument types do not match` failure. Re-ran the script successfully with the previously broken command forms for a single skill, a comma-separated skill list, an explicit PowerShell array call, and the default no-argument full regeneration path.
- Validation: `powershell -ExecutionPolicy Bypass -File .\scripts\sync-shared-skills.ps1 -Name lv0-home-auditor`, `powershell -ExecutionPolicy Bypass -File .\scripts\sync-shared-skills.ps1 -Name lv0-home-auditor,lv0-instruction-core`, `powershell -ExecutionPolicy Bypass -Command "& '.\scripts\sync-shared-skills.ps1' -Name 'lv0-home-auditor','lv0-instruction-core'"`, `powershell -ExecutionPolicy Bypass -File .\scripts\sync-shared-skills.ps1`, and a follow-up `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1` plus hash checks confirming the regenerated live-home wrapper files match the repo copies.
- Resume cues: the canonical shared-skill regeneration path is working again; future shared-skill edits should go through `.ai_shared/skills/` plus `scripts/sync-shared-skills.ps1` instead of requiring direct wrapper edits.

### 2026-03-10
- Goal: remediate the weekly-audit drift that was found in the March 10, 2026 `lv0-home-auditor` test run.
- Changed: updated `scripts/home-mirror.ps1` so `.ai_shared/skills/` is now part of the managed shared sync surface, refreshed the repo docs to describe `.ai_shared/skills/` as the canonical shared-skill location, removed the stale live-home `lv1-figma-implement-design` entries from `C:\Users\chang\.codex\skills\`, `C:\Users\chang\.codex\.agents-home\skills\`, `C:\Users\chang\.claude\skills\`, and `C:\Users\chang\.github\agents\`, deleted the managed-surface `DESKTOP-EF9LR8A` conflict-copy files in the repo, and ran a live `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1` so the current repo-managed shared layer and tool wrappers were pushed into `C:\Users\chang\`.
- Validation: confirmed `C:\Users\chang\.ai_shared\skills\` and the live-home `lv0-home-auditor` wrapper files now exist, verified hash matches for the root and tool entrypoint docs plus `.ai_shared/skills/catalog.json`, and confirmed the stale live-home `lv1-figma-implement-design` paths are absent.
- Resume cues: the weekly-audit drift from earlier on March 10, 2026 is resolved for the live home roots; the remaining tracked follow-up is the separate `scripts/sync-shared-skills.ps1` regeneration issue, which was not needed for this remediation.

### 2026-03-10
- Goal: run the first real `lv0-home-auditor` test pass for the calendar week of March 9-15, 2026 against this repo and the live home roots.
- Changed: treated the earlier 2026-03-10 skill-authoring entry as setup work rather than the weekly audit itself, then inspected the repo-local shared layer, top-level routing docs, and live home roots under `C:\Users\chang\`. The audit confirmed the shared folders are accessible to Codex, Claude, and Copilot and that the repo-local weekly-audit rules are in place, but it also found live-home drift: `C:\Users\chang\.ai_shared\skills\` does not exist, the new `lv0-home-auditor` wrapper files are not present in the live `.codex`, `.claude`, or `.github` roots, the live home entrypoint docs still differ from the repo copies, and the retired `lv1-figma-implement-design` remains active in the live home Codex, Claude, and Copilot wrapper roots.
- Validation: read the new `lv0-home-auditor` wrapper and continuity docs, verified the existence of the repo-local and live-home shared/tool roots, compared hashes for the repo and live-home entrypoint docs, confirmed the live-home absence of `.ai_shared/skills` and `lv0-home-auditor`, checked that old tool-root `knowledge/`, `templates/`, `workflows/`, and `checklists/` folders are no longer present in the live home roots, scanned the repo for conflict-copy files, and reviewed `scripts/home-mirror.ps1` to confirm that `.ai_shared/skills` is not currently part of the managed sync set.
- Resume cues: this week's audit ran successfully on Tuesday, March 10, 2026; the highest-value fixes are to decide whether `.ai_shared/skills` should be added to the sync surface, sync the current repo-managed wrappers into the live home roots, and clean or archive the stale live-home `lv1-figma-implement-design` entries plus the repo's many conflict-copy files.

### 2026-03-10
- Goal: add a shared `lv0-home-auditor` layer that performs the first-repo-request weekly audit for home-folder alignment and repo-specific rule checks.
- Changed: added the canonical shared `lv0-home-auditor` definition to `.ai_shared/skills/catalog.json` and `.ai_shared/skills/lv0-home-auditor/shared.md`, added the shared `workflow-home-audit.md` and `home-audit-checklist.md`, updated the shared stack and top-level Codex, Claude, and Copilot routing docs to invoke the auditor on the first repo request of a calendar week, created the Codex, Codex home-library, Claude, and Copilot wrapper files for the new skill, and updated each tool's `lv0-instruction-core` wrapper so the weekly gate and canonical `.ai_shared/skills/` regeneration rule are visible at the entry layer.
- Validation: confirmed the new wrapper paths exist under `.codex/skills/`, `.codex/.agents-home/skills/`, `.claude/skills/`, and `.github/agents/`; scanned the shared and top-level docs for `lv0-home-auditor` references; and previewed a full home sync with `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1 -WhatIf`.
- Resume cues: the weekly audit behavior is now documented and discoverable across the shared layer plus all three tool roots; if another shared skill is added soon, double-check the wrapper-regeneration path because the non-interactive `sync-shared-skills.ps1` run in this session did not materialize the new wrapper files and the wrappers were added directly in the repo.

### 2026-03-09
- Goal: finish the continuity-doc migration so old home-level tracker files are removed and cannot be reintroduced by sync.
- Changed: updated `scripts/home-mirror.ps1` so `knowledge/progress-tracker.md` and `knowledge/future-plan.md` are excluded during home syncs, verified the live home copies matched the repo-local versions, deleted the stale duplicates from `C:\Users\chang\.codex\knowledge\`, `C:\Users\chang\.claude\knowledge\`, and `C:\Users\chang\.github\knowledge\`, and confirmed a fresh sync did not recreate them.
- Validation: compared file hashes between repo-local and live-home continuity docs before deletion, reran `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1`, and verified the three live home `knowledge/` folders no longer contain `progress-tracker.md` or `future-plan.md`.
- Resume cues: repo-local continuity docs now exist only in repo folders; if another repo adopts the same pattern, keep the files in that repo and do not add them back to the shared home mirrors.

### 2026-03-09
- Goal: move repo continuity and other repo-specific guidance out of home knowledge folders and into each repo's local tool folders.
- Changed: updated the shared lv0 routing docs, repo-onboarding guidance, task skills, and mirrored root instructions across `.codex`, `.claude`, `.github`, and the Codex home-library mirror so `progress-tracker.md`, `future-plan.md`, and other repo-specific knowledge or template overrides now live under the current repo's `.codex/`, `.claude/`, or `.github/` tree instead of the home knowledge folders.
- Validation: rescanned the repo for stale references to writing `progress-tracker.md` or `future-plan.md` into `~/.codex/knowledge`, `~/.claude/knowledge`, or `~/.github/knowledge`, and spot-checked the updated top-level instructions plus the lv0 and lv1 wrappers in each tool root.
- Resume cues: shared home folders now act as cross-repo defaults only; when onboarding another repo, create or refresh that repo's local continuity docs and keep repo-specific templates or checklists beside them.

### 2026-03-09
- Goal: make cross-tool alignment an explicit base-layer rule whenever a shared skill or agent changes in one toolset.
- Changed: updated `lv0-instruction-core` in `.codex/skills/`, `.codex/.agents-home/skills/`, `.claude/skills/`, and `.github/agents/` so the shared routing layer now requires the matching Codex, Claude, and Copilot skill or agent to be updated or explicitly verified in the same pass.
- Validation: compared the edited `lv0-instruction-core` wrappers across all three tool roots and the Codex home-library mirror, then synced the managed files with `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1`.
- Resume cues: treat this as the default shared-maintenance rule going forward; if a future change intentionally leaves one toolset different, call out the wrapper-specific reason in the handoff and tracker entry.

### 2026-03-09
- Goal: realign the three home tool roots so Codex, Claude, and Copilot expose the same shared framework shape and active layer names.
- Changed: refreshed `.codex/AGENTS.md` to the current shared routing model with continuity docs and PRD guidance, expanded `.github/copilot-instructions.md` so Copilot now documents the same `lv0` and `lv1` stack and task-routing pattern as the other roots, and removed the leftover Claude `skills/github-open-source-readiness/` folder from both the repo mirror and `C:\Users\chang\.claude` so the active shared capability set cleanly matches `lv1-github-repo-readiness`.
- Validation: compared the shared folder sets and hashes across `.codex`, `.claude`, and `.github`, confirmed checklists, templates, and workflows are aligned, reran targeted scans for `github-open-source-readiness` and `lv1-github-repo-readiness`, previewed the managed sync with `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1 -WhatIf`, ran the live `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1`, and confirmed the active `lv0` and `lv1` directories now match across all three roots.
- Resume cues: when another shared layer changes later, update the Codex root instructions, the Claude and Copilot wrappers, and any stale legacy folders in the same pass so the three tool roots stay logically equivalent.

### 2026-03-09
- Goal: retire the shared Figma design skill from the local repo mirrors because this machine cannot use the Figma integration path.
- Changed: removed the active `lv1-figma-implement-design` skill files from `.codex/skills/` and `.codex/.agents-home/skills/`, added lightweight archive markers under both `_removed-skills/` folders, updated the shared router docs so Codex no longer advertises the Figma capability alongside the aligned root, Claude, and Copilot surfaces, synced the updated Codex files into `C:\Users\chang\.codex`, and deleted the retired live home skill folders there.
- Validation: confirmed the active repo and live-home Codex skill paths are absent, confirmed the archive-marker paths exist in the repo mirror, reran focused discovery-doc scans in both locations with no active Figma references, and ran `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1 -Tool codex`.
- Resume cues: if Figma access is restored later, recover the retired skill from git history or a previous shared snapshot instead of rebuilding it manually.

### 2026-03-09
- Goal: ensure the latest shared Figma/GitHub skill and feature-dev PRD workflow commits are fully applied on the local home machine.
- Changed: confirmed the repo mirror is current with `origin/main` at `8c4e172`, verified the live home copies for every path touched by commits `0d7b736` and `8c4e172`, found that `C:\Users\chang\AGENTS.md` was the only missing target because `scripts/home-mirror.ps1` did not sync root-level managed files, and updated the mirror script so `AGENTS.md` now syncs in both directions.
- Validation: `git -C E:\Dropbox\project\agent-setup fetch origin --prune`, a file-hash comparison of every path touched by `0d7b736` and `8c4e172` against `C:\Users\chang`, `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1`, and a post-sync compare confirming the targeted home files now match the repo, including `C:\Users\chang\AGENTS.md`.
- Resume cues: if another shared home-root file needs to travel between the repo mirror and the live machine, add it to the root item list in `scripts/home-mirror.ps1` instead of copying it manually.

### 2026-03-09
- Goal: teach the shared `lv1-feature-dev` flow to capture lightweight PRDs when feature work surfaces requirements, APIs, or use cases.
- Changed: updated the feature-dev skill, feature workflow, feature-validation checklist, top-level routing docs, and agent-stack notes across `.codex`, `.claude`, and `.github`; added a reusable `prd-template.md` to each tool root; updated the Codex picker prompt and home-library mirror so feature work can create or update PRDs alongside implementation.
- Validation: `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1 -WhatIf`, `Get-FileHash .codex\skills\lv1-feature-dev\SKILL.md, .codex\.agents-home\skills\lv1-feature-dev\SKILL.md, .codex\skills\lv1-feature-dev\agents\openai.yaml, .codex\.agents-home\skills\lv1-feature-dev\agents\openai.yaml | Format-Table -AutoSize`, `Get-ChildItem AGENTS.md, .codex, .claude, .github -Recurse -File | Select-String -Pattern 'prd-template|PRD' | Select-Object Path, LineNumber, Line | Format-Table -AutoSize`, and `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1`.
- Resume cues: feature work should now prefer an existing PRD or docs location when one exists and fall back to `docs/prd/<feature-name>.md` with the shared template when requirements need to be recorded.

### 2026-03-09
- Goal: reconcile the Dropbox repo's local `main` with `origin/main` after fixing the object-store permission issue.
- Changed: confirmed the local commit `5c8b9d8` and remote commit `ee38fb1` had the same parent and tree, created backup branch `codex/main-before-reconcile` at the local-only commit, and moved `main` to `ee38fb1` so the working branch matches `origin/main` again without disturbing the uncommitted tracker updates.
- Validation: `git -C E:\Dropbox\project\agent-setup diff --stat 5c8b9d8 ee38fb1`, `git -C E:\Dropbox\project\agent-setup cherry -v origin/main main`, `git -C E:\Dropbox\project\agent-setup update-ref refs/heads/main ee38fb1 5c8b9d8`, and `git -C E:\Dropbox\project\agent-setup status -sb`.
- Resume cues: `main` is aligned with `origin/main`; if the older local commit is ever needed, it is preserved on `codex/main-before-reconcile`.

### 2026-03-09
- Goal: restore reliable git operations in the Dropbox-managed `agent-setup` repo after `.git/objects` writes started failing with permission errors.
- Changed: traced the failures to Dropbox cloud-file reparse points under `.git`, copied `.git/objects` to `C:\Users\chang\.git-object-stores\agent-setup-objects`, renamed the original folder to `E:\Dropbox\project\agent-setup\.git\objects.dropbox-backup-20260309`, and replaced `.git/objects` with a junction to the external store so git object writes bypass Dropbox virtualization.
- Validation: `git -C E:\Dropbox\project\agent-setup add AGENTS.md .codex .claude .github`, `git -C E:\Dropbox\project\agent-setup fetch origin`, `git -C E:\Dropbox\project\agent-setup fetch C:\Users\chang\tmp\agent-setup refs/heads/codex-fetch-test:refs/remotes/codex-fetch-test`, and `git -C E:\Dropbox\project\agent-setup status -sb`.
- Resume cues: git object reads and writes now go through the external store, but the local `main` branch is still diverged from `origin/main` (`5c8b9d8` locally versus `ee38fb1` on origin), which is a separate history-reconciliation task.

### 2026-03-09
- Goal: promote the shared Figma design and GitHub readiness capabilities into reusable `lv1` skills that multiple repos can call safely.
- Changed: added `lv1-figma-implement-design` and `lv1-github-repo-readiness` under `.codex/skills/` and `.codex/.agents-home/skills/`, added the reusable GitHub checklist and audit script, broadened the GitHub readiness guidance so it supports both private and public repos, and updated the shared stack and discovery docs across `.codex`, `.claude`, `.github`, and the root `AGENTS.md`.
- Validation: `powershell -ExecutionPolicy Bypass -File .\\scripts\\sync-to-home.ps1 -WhatIf`, SHA256 mirror comparison between `.codex/skills/lv1-*` and `.codex/.agents-home/skills/lv1-*`, `powershell -ExecutionPolicy Bypass -Command "& '.\\.codex\\skills\\lv1-github-repo-readiness\\scripts\\audit-repo.ps1' -RepoPath '.' -Visibility either"`, and a `Select-String` scan that found no managed-surface references to the legacy exact names ``github-open-source-readiness`` or ``figma-implement-design``.
- Resume cues: the repo mirror, the git-backed home mirror, and the live home folders now all expose the new `lv1` skills; use `lv1-figma-implement-design` directly for shared design-to-code work and `lv1-github-repo-readiness` for private or public GitHub audits.

### 2026-03-09
- Goal: standardize repo continuity docs so every shared skill stack maintains a progress tracker and a future plan by default.
- Changed: updated the shared `lv0` core, repo-onboarding guidance, workflows, and top-level instructions across `.codex`, `.claude`, and `.github`; added the two continuity docs to each tool root.
- Validation: `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1 -WhatIf`, a `Select-String` scan for `progress-tracker` and `future-plan` across `.codex`, `.claude`, and `.github`, and a live `powershell -ExecutionPolicy Bypass -File .\scripts\sync-to-home.ps1` sync all completed successfully.
- Resume cues: if the tracker pattern feels too manual later, add a thin helper or template instead of another workflow layer.
