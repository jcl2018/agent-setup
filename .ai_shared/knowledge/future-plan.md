# Future Plan

## Purpose
Capture unfinished work, deferred ideas, and next-step todos that should survive across sessions.

## Update Rules
- Add concrete action-oriented items with enough context to resume quickly.
- Remove, mark done, or move items into `progress-tracker.md` once they are completed.
- Keep the highest-value near-term work near the top.

## Current Follow-Ups
- Decide whether to extend the shared generator approach to root entry docs such as `AGENTS.md`, `.claude/CLAUDE.md`, and `.github/copilot-instructions.md`, or keep those as hand-maintained thin wrappers.
- Decide whether to add a dedicated prune or migration helper so future `.ai_shared`-style restructures do not require manual legacy-folder cleanup after sync.
- After a few real feature tasks use the shared PRD flow, decide whether the shared template needs a lighter one-page variant or a repo-level PRD index pattern.
- After a little soak time, decide whether to keep or remove the backup object store at `E:\Dropbox\project\agent-setup\.git\objects.dropbox-backup-20260309`.
- Watch whether the new shared continuity docs need a lighter template after a few real tasks use them.
