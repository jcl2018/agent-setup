interface:
  display_name: "Lv0 Home Auditor"
  short_description: "Run the weekly home-folder alignment audit"
  default_prompt: "Use $lv0-home-auditor to check the global home-audit gate and the current repo's weekly audit gate, run the shared local-repo audit baseline, apply any repo-local audit rules, compare managed mirror surfaces to the live home roots when relevant, and summarize cleanup candidates without deleting anything automatically."

policy:
  allow_implicit_invocation: true
