interface:
  display_name: "Lv1 Feature Dev"
  short_description: "Implement scoped feature work and capture PRD details when needed"
  default_prompt: "Use $lv1-feature-dev to inspect context, capture requirements in a PRD when APIs or use cases need to be recorded, plan the smallest safe feature change, implement it, validate it, and summarize the result."

policy:
  allow_implicit_invocation: true
