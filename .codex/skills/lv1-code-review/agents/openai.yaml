interface:
  display_name: "Lv1 Code Review"
  short_description: "Review changes for correctness, regressions, and test gaps"
  default_prompt: "Use $lv1-code-review to inspect the change, identify findings ordered by severity, note missing validation, and summarize residual risk."

policy:
  allow_implicit_invocation: true
