interface:
  display_name: "Lv1 Defect Fix"
  short_description: "Diagnose and fix a scoped bug"
  default_prompt: "Use $lv1-defect-fix to inspect the bug, find the likely root cause, apply the smallest reliable fix, validate it, and summarize any remaining risk."

policy:
  allow_implicit_invocation: true
